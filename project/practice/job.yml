apiVersion: batch/v1
kind: Job

metadata: 
  name: job 

spec: 
  template:
    spec:
      containers:
      - name: busybox
        image: busybox:latest
        command: ["sh", "-c"]
        args:
        - |
          echo "Hello Dosto"
          sleep 20
      restartPolicy: Never
